// String, number, undefined, null, boolean, symbol (symbol será explicado nas próximas aulas.)
const nome = 'Alexandre'; //string
const nome1 = "Alexandre"; //string
const nome2 = `Alexandre`; //string
const number = 10; //number
const number2 = 10.52; //number
let nomeAluno; // undefined -> não aponta para local nenhum na memória
const sobrenomeAluno = null; // Nulo -> não aponta para local nenhum na memória
const aprovado = false; // Boolean = true, falsa (lógico)


